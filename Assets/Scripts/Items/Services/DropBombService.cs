using UnityEngine;

public class DropBombService : MonoBehaviour
{

    public DropBombService(string name, string description, int cost, Sprite icon = null)
    {

    }

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }


}
